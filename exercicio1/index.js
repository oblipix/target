// Código para calcular o valor da variável SOMA
let INDICE = 13;
let SOMA = 0;
let K = 0;

while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

console.log(`O valor de SOMA ao final do processamento é: ${SOMA}`); // Saída: 91
